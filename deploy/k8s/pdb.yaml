apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: lucrum-web-pdb
  namespace: lucrum
  labels:
    app: lucrum-web
    app.kubernetes.io/name: lucrum-web
    app.kubernetes.io/part-of: lurus
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: lucrum-web
