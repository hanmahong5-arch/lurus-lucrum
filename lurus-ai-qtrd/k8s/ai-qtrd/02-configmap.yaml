# Lucrum ConfigMap
# Non-sensitive configuration for the application
apiVersion: v1
kind: ConfigMap
metadata:
  name: lucrum-config
  namespace: lucrum
  labels:
    app.kubernetes.io/name: lucrum
    app.kubernetes.io/component: config
data:
  # Server configuration
  WEB_HOST: "0.0.0.0"
  WEB_PORT: "8000"
  LOG_LEVEL: "info"

  # DeepSeek API configuration (via lurus-api gateway)
  DEEPSEEK_API_BASE: "https://api.lurus.cn/v1"
  DEEPSEEK_MODEL: "deepseek-chat"

  # CORS configuration
  CORS_ORIGINS: '["https://lucrum.lurus.cn"]'

  # Frontend URL for redirects
  WEB_URL: "https://lucrum.lurus.cn"
  API_URL: "https://lucrum.lurus.cn/api"

  # Data paths
  DATA_DIR: "/app/data"
  LOG_DIR: "/app/logs"
